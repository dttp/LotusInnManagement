<div ng-controller="userList" ng-init="init()">
    <!-- BEGIN PAGE HEADER-->
    <h3 class="page-title">
        Users Management
    </h3>
    <div class="page-bar">
        <ul class="page-breadcrumb">
            <li>
                <i class="fa fa-home"></i>
                <a href="\">Dashboard</a>
                <i class="fa fa-angle-right"></i>
            </li>
            <li>
                <a href="#">Users</a>
            </li>
        </ul>
    </div>
    <!-- END PAGE HEADER-->
    <div ng-repeat="item in alertSvc.alerts" class="alert alert-{{item.type}}">
        <button type="button" class="close" data-dismiss="alert" aria-hidden="true" ng-click="alertSvc.remove(item)"></button>
        <strong>{{item.title}}</strong> {{item.body}}
    </div>
    <!-- BEGIN PAGE CONTENT-->
    <div class="row">

        <div class="col-md-12">
            <div class="row">
                <div class="col-md-12">
                    <button type="button" class="btn btn-primary" ng-click="create()"><i class="fa fa-plus" aria-hidden="true"></i> Create</button>
                </div>
            </div>
            <div class="row">
                <div class="col-md-12">
                    <div class="portlet">
                        @*<div class="portlet-title">
                                <div class="caption">

                                </div>
                            </div>*@
                        <div class="portlet-body">
                            <div class="table-scrollable">
                                <table class="table table-bordered table-hover table-striped">
                                    <thead>
                                        <tr>
                                            <th>Name</th>
                                            <th>Email</th>
                                            <th>Phone</th>
                                            <th>House</th>
                                            <th>Role</th>
                                            <th>Status</th>
                                            <th>Actions</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        <tr ng-if="users.length == 0">
                                            <td colspan="6">
                                                There are no items
                                            </td>
                                        </tr>
                                        <tr ng-repeat="user in users">
                                            <td>
                                                <a href="/users/detail?id={{user.Id}}">{{user.Username}}</a>
                                            </td>
                                            <td>{{user.Email}}</td>
                                            <td>{{user.Phone}}</td>
                                            <td>{{user.House.Name}}</td>
                                            <td>{{user.Role.Name}}</td>
                                            <td><span ng-if="user.Status && user.Status.length > 0" class="label" ng-class="{'label-success': user.Status == 'Verified', 'label-warning': user.Status == 'NotVerified', 'bg-grey-gallery': user.Status=='Disabled'}">{{user.Status}}</span></td>
                                            <td>
                                                <a ng-if="user.Id != 'u-admin'" href="javascript:;" title="Reset password" class="btn btn-icon-only yellow-crusta" ng-click="resetPassword(user)">
                                                    <i class="fa fa-refresh"></i>
                                                </a>
                                                <a ng-if="user.Id != 'u-admin'"href="javascript:;" title="Edit this user"class="btn btn-icon-only blue-hoki" ng-click="edit(user)">
                                                    <i class="fa fa-edit"></i>
                                                </a>
                                                <a ng-if="user.Id != 'u-admin'" href="javascript:;" title="Delete this user" class="btn btn-icon-only red-flamingo" ng-click="delete(user)">
                                                    <i class="fa fa-remove"></i>
                                                </a>
                                            </td>
                                        </tr>
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <!-- END PAGE CONTENT-->
</div>
@section AngularController{
    
    <script src="~/app/components/users/userlist.js"></script>
}
